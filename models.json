{
  "providers": {
    "anthropic": {
      "baseUrl": "http://localhost:6655/anthropic",
      "api": "anthropic-messages",
      "apiKey": "!python3 -c \"import json, pathlib; print(json.loads(pathlib.Path('~/.pi/agent/auth.json').expanduser().read_text()).get('anthropic',''))\"",
      "authHeader": true
    }
  }
}
